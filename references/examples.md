# 提示词示例

以下均为虚构教学素材，不代表真实仓库、接口或验证结果。路径与命令只在示例输入明确提供后使用；输出中的验证是待执行要求。

## 1. 简单修改：保持短小

**用户输入**

> 用这个 skill 整理成执行提示词：把 `web/src/components/Welcome.vue` 中主按钮的“开始体验”改成“创建项目”。只改这处文案，已有样式和点击行为已经通过验收。

**可直接交接的输出**

```text
修改 web/src/components/Welcome.vue，将主按钮文案“开始体验”改为“创建项目”。先确认目标元素；若文件或文案不匹配，报告差异，勿猜测替换位置。
范围仅限这一处文案，保留已验收的样式和点击行为。检查 diff 确认无其他改动；按项目现有预览方式检查按钮实际显示，并如实说明运行验证是否完成。
```

说明：单点修改只需目标、范围和适度验证，无需展开完整任务书。

## 2. 复杂接口任务：保留有效决定与验收边界

**用户输入**

> 用这个 skill 整理为实现提示词。已知文件为 `src/views/TaskList.vue` 和 `src/api/tasks.ts`。
> 早先同意使用 `DELETE /api/tasks/:id` 删除任务，但后来已撤销这一决定，最终只做归档，禁止实现删除或增加新接口。
> 已确认契约：`POST /api/tasks/:id/archive` 无请求体，成功返回 `200 { id, archived: true }`；`403` 表示无权限。
> 超时后可用 `GET /api/tasks/:id` 查询，返回 `{ id, archived }`。只有同一任务 id 且 archived 为 true 才能确认归档成功。
> 请求期间禁止重复提交；超时不可自动重试写入，也不可直接显示成功。查询失败或 archived 为 false 时保持结果待确认，不重新开放归档提交。
> 本轮只改这两个文件。列表布局已经通过浏览器验收，保留布局，只补归档操作及其状态；其他页面、后端均不在范围内。
> 项目已有 `pnpm typecheck` 和 `pnpm test:unit`。验收还必须实际操作浏览器，覆盖成功、连点、403、超时后的查询确认；不能用静态检查代替。

**可直接交接的输出**

```text
目标与范围
在 src/views/TaskList.vue 和 src/api/tasks.ts 接入任务归档及其反馈状态，仅修改这两个文件；保留已通过浏览器验收的列表布局。其他页面、后端不在范围内。
旧的 DELETE /api/tasks/:id 删除方案已撤销，不得沿用；禁止增加接口。

契约与行为
- 使用用户已确认的 POST /api/tasks/:id/archive，无请求体；200 返回 { id, archived: true }。修改前核对现有调用约定与契约，发现冲突时明确指出，勿自行替换契约。
- 提交期间禁止重复提交；响应必须对应原任务 id 才可更新该任务的归档状态。
- 403 显示无权限，不显示成功。
- 超时进入结果待确认状态，不自动重试写入；使用 GET /api/tasks/:id 查询 { id, archived }。
- 只有查询的 id 与原任务一致且 archived 为 true，才确认成功；查询失败、id 不符或 archived 为 false 时继续待确认，保持归档提交禁用。
- 复用项目现有请求、状态和错误展示方式，不预先指定新的内部架构。

验证与交付
运行 pnpm typecheck 和 pnpm test:unit；若检查需要范围外修复，报告问题，不扩大本轮修改范围。
实际操作浏览器分别验证：成功后正确更新原任务；连点只产生一次写请求；403 不显示成功；超时不误报成功且不重试写入；查询仅在原 id 和 archived 为 true 时确认成功，其余情况继续禁用提交。
检查列表布局保持原样，diff 仅涉及指定文件。交付改动摘要、检查结果及浏览器步骤和证据；将通过、失败、未执行的项目分别列明。
```

说明：压缩讨论时明确保留后续撤销决定、已批准范围和真实浏览器验收要求。

## 3. 信息不足的 Bug：先诊断，不固化猜测

**用户输入**

> 帮我生成给另一位代理的提示词：编辑页保存偶发要点两次，我猜是缓存。还没有收集日志，不知道是哪段代码，也没有稳定复现步骤。先查明原因，暂时不要修改代码。

**可直接交接的输出**

```text
先诊断“编辑页保存偶发需要点击两次”的问题，本轮不修改代码。
已知：用户报告了上述现象，但尚无日志和稳定复现步骤。“缓存导致”仅为用户假设，不得作为已确认根因。

先检查仓库说明和已有运行配置，发现编辑页、保存入口、请求链路及实际可用的启动/检查命令；不要猜测文件路径或命令。若无法定位具体编辑页，先询问页面入口，同时继续可独立进行的仓库梳理。
尝试复现并记录操作顺序、两次点击对应的请求与响应、页面状态及相关错误。结合证据检验缓存假设，也检查其他解释，勿直接按缓存问题设计修复。
输出复现步骤、相关代码位置、证据、已排除与未证实的假设，并说明是否找到根因。不能复现或证据不足时如实报告，指出下一项最有区分力的检查；有依据后可给出最小修复建议，但不要执行修改。
```

说明：信息不足时先把诊断目标和证据要求交接清楚，不把猜测改写成实现命令。
