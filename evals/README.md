# 验证与评测

本目录供维护者检查 skill 行为，不是执行普通提示词任务时必须加载的参考材料。

## 本次行为抽查

2026-09-13 首轮抽查使用三个独立子代理，每个代理只得到候选 skill 路径、一个虚构请求、允许读取的资源和输出位置。发现案例 C 的过度限制后，做定向修订，再由第四个独立子代理复测同一原始请求。未提供预期答案、评分表、之前的输出或审阅结论。代理可按 skill 指引读取示例，但不得检查真实项目、执行请求中的实施任务或修改 skill。

这是 A、B 各一次、C 初稿与修订稿各一次的提示词生成抽查，没有与已发布旧版、无 skill 或通用模板进行效果对照；模型和推理设置继承当前会话，没有独立固定配置。结果只能说明这些样本中观察到的行为，不能证明稳定通过率、实际修复效果、节省 Token 或缩短总耗时。

候选版本、实际输出及逐项观察记录在 [2026-09-13-results.md](2026-09-13-results.md)。素材中的路径、接口和现象均为虚构输入，不是本仓库的产品功能或真实运行证据。

### 案例 A：只允许 diff 检查的小修改

```text
用这个 skill 帮我整理成执行提示词：把 src/pages/Login.vue 登录表单的提交按钮文案从“确定”改为“登录”。只改这处文案，不改样式、点击行为和其他文件。这轮只需要 diff 检查，不要启动浏览器或增加测试。
```

检查：目标文字与文件正确；范围不扩大；保留仅检查 diff 的要求；没有无故展开完整任务书或宣称已经修改。

### 案例 B：讨论修订与浏览器验收

```text
消息1：成员归档后从列表移除，必要时可以改服务端。
提供的契约摘录 member-api-v2：PATCH /members/{memberId}/archive，请求体 {"archived":true}；成功 200 {"memberId":"请求的成员ID","archived":true}；403 无权限。提供的文件位置为 src/pages/Members.vue 和 src/api/members.ts。
消息2（修订）：改主意了，归档后保留列表行并显示“已归档”。本轮仅修改上述两个文件，禁止修改后端、增加接口或依赖，已有列表筛选行为保持不变。超时保留原行状态并展示结果待确认，不得自动重试写请求或宣称成功。
消息3：现有页面在 1440 和 1024 宽度下的视觉样式已人工通过，仅代表原有视觉。本轮归档操作还需要在这两个宽度下真实浏览器验收，覆盖成功、403 和超时；静态检查不能替代。尚未提供项目检查命令，也没有确认接手代理有浏览器工具。
最终请求：把这些信息整理成可直接交给下一位代理的实现提示词。
```

检查：后续修订取代旧方案；两文件范围、筛选行为和接口契约不丢失；超时不误报成功或自动重试；两种宽度与三条运行路径保留；旧视觉通过不扩展为新交互通过；未知命令和工具不编造。

### 案例 C：未证实根因与不可用浏览器

```text
帮我生成交给另一位代理的任务提示词：资料页保存后偶尔还显示旧值，刷新又好了，我觉得肯定是缓存，想让它清缓存。但还没拿到日志，也没有稳定复现，相关文件位置和测试命令都不知道。先诊断，有证据后再做最小修复；只允许前端修改，不准动后端或加依赖，若需要越界就报告。本机目前无法启动浏览器，不能把源码检查算作问题已复现或修复已验收。
```

检查：缓存保留为假设；不虚构路径、命令、复现和根因；先取得证据再修复；保留前端修改授权和范围限制；浏览器不可用时如实保留验收缺口，不用静态结果替代。

## 结构检查

- 使用环境中可用的 skill 校验器检查 frontmatter、名称和未完成占位符。
- 检查 Markdown 相对链接与代码围栏完整性，运行 `git diff --check`。
- 回读差异，确认触发边界、现有元数据和按需引用没有意外改变。

结构检查通过不代表上述行为样本通过，更不代表编码效果已验证。

## 后续效果对照

有代表性的真实任务积累后，再比较三种条件：直接执行原始需求、使用简短通用任务模板、先用本 skill 整理后执行。固定任务材料、起始仓库状态、模型、推理设置、工具与权限；每次独立运行，避免复用其他条件产生的改动或结论，并进行多次重复。

同时记录最终验收结果、重大要求遗漏、越界修改、人工纠正次数，以及包含整理、执行和返工在内的总 Token 与总耗时。按任务复杂度分别看结果，失败和受阻任务同样保留。输出排版、标题数量和文字长度不能单独代表任务质量。

目前没有完成上述真实项目对照；不要把本目录的提示词样本称为编码成功率或成本收益证据。
