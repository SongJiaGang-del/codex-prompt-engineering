# 2026-09-13 提示词行为抽查记录

范围：`PROMPT_ONLY`。没有执行虚构项目的实现、诊断或浏览器验收，也没有测量总 Token 或端到端耗时。

## 被测版本与方法

- 已发布起点：`f1ce0e5`；该起点未作为本次行为对照运行。
- 首轮候选核心规则 Git blob：`d7404e56e504f0f996117abfff7d38377f1673aa`。
- 首轮核心规则 LF-SHA256：`60acdc16f8bf0ca9c3e80ced900ab1a883c9b55b974366a555a5d396fbf4d877`。
- 修订后核心规则 Git blob：`272299835b7740f4b66f691872d03aa200c6b57c`。
- 修订后核心规则 LF-SHA256：`01a386ecfda94fb51f5cf799490cb05dd768b04bdde00553ab9e28b92d787084`。
- 两轮共用示例 LF-SHA256：`751d69690732fa89571601c1e3f2d71b434e1ef244efdc71ae1bc2e65d25d834`。
- SHA256 以 UTF-8 文本、换行统一为 LF 后计算，避免 Git 在 Windows 转换换行造成歧义。
- 素材、检查标准、执行边界及效果评测限制见 [README](README.md)。输出由四个独立子代理生成，由主代理逐项阅读评估；评估判断仍可能存在主观偏差。

## 观察与修订

| 样本 | 被测版本 | 观察结果 |
| --- | --- | --- |
| A：单点文案 | 首轮候选 | 保留准确文件、替换文字和唯一修改范围；只做 diff 检查，未要求启动浏览器或新增测试；保持一段短指令。 |
| B：讨论修订 | 首轮候选 | 明确撤回移除行和改服务端的旧决定；保留两文件范围、接口、筛选与超时限制，以及两个宽度各三种场景；正确限定旧视觉批准与当前未验证状态。 |
| C：根因不明 | 首轮候选 | 保留假设与浏览器缺口，但额外写出“任何缓存相关处理都须先说明与已证实原因的关系”，可能把可逆诊断试验也挡住，需要修订；验证边界也有重复。 |
| C：根因不明 | 修订后候选 | 区分可逆诊断试验与未经证实的正式修复，保留证据充分后的前端修改授权和浏览器验收缺口；未编造路径或命令。该样本未再出现首轮的全面限制。 |

定向修订仅涉及核心规则两处：Fix 模式明确允许授权范围内的可逆诊断试验；Delivery 要求同一约束尽量只写一次。A、B 未在最终候选上重复生成，不能把其首轮结果标成最终版本的全量回归通过。复测 C 仍只是一条样本，不能据此宣称问题在所有输入下消失。

首轮与复测都没有发现将提示词生成结果宣称为真实项目完成或已验收的情况。B、C 的篇幅仍较长；本次未做用户阅读成本或相同输入多次运行的稳定性评估。

## 实际生成输出

以下保留各代理写入的提示词正文；未将主代理的审阅意见回写进样本。独立代理仅生成这些文本，没有执行其中描述的任务。

### A：单点文案（首轮）

输出 LF-SHA256：`383940dc87e1a52a53eedd15cdb7eaeca2123ff24fe3dd57d187668269b2991f`。

```text
在 `src/pages/Login.vue` 中定位登录表单的提交按钮，将文案从“确定”改为“登录”。仅修改这一处文案，不改样式、点击行为、文件内其他内容或其他文件。完成后只检查 diff，确认本轮改动仅包含该处文案替换，并报告修改位置及 diff 检查结果；不要启动浏览器，不要运行或增加测试。
```

### B：讨论修订（首轮）

输出 LF-SHA256：`1f384f6bd133c3136869bd2b3c3c3fe8c5e121194735527a823e5e335287f548`。

```text
请在接手的项目中实现成员归档交互，并按以下边界完成验证。

## 当前目标与范围

- 归档成功后保留成员列表行，并显示“已归档”。早期“归档后移除列表行”和“必要时可改服务端”的决定已被撤回，不得沿用。
- 本轮仅允许修改 `src/pages/Members.vue` 和 `src/api/members.ts`。禁止修改后端、增加接口或依赖；已有列表筛选行为保持不变。
- 用户报告现有页面在 1440 和 1024 宽度下的视觉样式已人工通过，此结论仅覆盖原有视觉，不代表本轮归档交互已通过。沿用现有样式和组件惯例，避免无关改动。

## 已提供的接口契约

以下内容来自用户提供的 `member-api-v2` 摘录，尚未由接手代理检查项目实现：

- `PATCH /members/{memberId}/archive`
- 请求体：`{"archived":true}`
- 成功：HTTP 200，响应体 `{"memberId":"请求的成员ID","archived":true}`
- HTTP 403：无权限。

先只读检查目标文件、相关调用与筛选逻辑，确认现有请求封装和错误处理方式。使用上述契约完成归档，不自行发明接口；若实际实现与契约不一致，报告具体差异，不擅自扩展修改范围。

## 交互要求

1. 针对用户选择的成员发送归档请求。收到符合契约的 200 响应，且响应 `memberId` 与请求成员一致、`archived` 为 `true` 后，保留该列表行并显示“已归档”。
2. 收到 403 时展示无权限反馈，保留原行状态，不标记归档成功。
3. 请求超时时保留原行状态，并展示“结果待确认”。超时不能证明写入成功或失败；不得自动重试写请求，不得宣称成功。检查现有请求封装是否会自动重试，确保此操作符合限制。
4. 保持已有列表筛选行为。若检查发现现有筛选规则会与“归档成功后保留列表行”发生实际冲突，说明具体规则与冲突，并请用户明确预期后再实现冲突部分；可继续独立且已明确的工作，不自行修改筛选语义。

## 验证与证据

- 尚未提供项目检查命令。先从项目现有说明、脚本和运行环境中确认适用命令及调用方式，再执行与两处修改有关的检查；不得猜测包管理器、命令或测试工具，也不得新增依赖来补齐环境。
- 开始时确认是否具备真实浏览器工具、页面启动条件，以及成功、403、超时的可复现方式。当前不能假定这些条件已经具备。
- 必须在真实浏览器中分别以 1440 和 1024 宽度执行归档操作，每个宽度均覆盖成功、403、超时，共六组场景。逐项观察并记录：成功后原行保留且显示“已归档”；403 后原行状态不变且出现无权限反馈；超时后原行状态不变且显示“结果待确认”，无成功提示、无自动重试写请求。结合实际请求记录核对接口、请求成员和重试情况，并检查已有筛选行为及原有视觉是否受到影响。
- 记录实际视口、复现步骤、观察结果及对应证据；如通过受控响应复现场景，明确标注模拟范围，不将其说成真实后端验收。
- 静态检查、构建或测试通过，以及原有视觉的人工通过记录，均不能替代本轮真实浏览器验收。
- 若浏览器工具或运行条件缺失，继续完成可独立开展的范围内实现与检查，将未执行场景标为 `UNVERIFIED` 或阻塞，说明原因、缺少的条件和剩余验收项；不得宣称本轮完整验收通过。

## 交付

提交仅涉及上述两个文件的聚焦修改，并在最终报告中说明行为变化、实际执行的检查命令与结果、六组浏览器场景的验证状态及证据、任何尚待澄清或未完成的验收项。区分实现完成、静态检查完成和浏览器验收完成，不将提示词、旧视觉批准或未执行的计划作为通过证据。
```

### C：根因不明（首轮，发现问题）

输出 LF-SHA256：`e5e1b3b48fdc5b1e31e6335ce61c955497c97eee6af403cd45f7104ec8c5486d`。

```text
请先诊断“资料页保存后偶尔仍显示旧值，刷新后恢复”的问题，在证据足够时做最小前端修复。

### 已知情况与目标

- 这是用户报告的间歇性现象，尚未获得日志，也没有稳定复现；本任务尚无已验证的根因。
- 用户猜测是缓存并提出清缓存，但这只是待验证假设，不能直接转化为清缓存的实施要求。
- 目标是查明保存后显示旧值的原因；若证据支持且修复完全位于允许范围内，使资料保存成功后的显示与已确认保存的值一致，无须刷新才能更新。

### 范围与约束

- 只允许修改前端，不得修改后端或新增依赖；保留现有接口契约和无关行为。
- 先诊断，后修复。没有足够证据时不提交猜测性修复，不预先清除浏览器、应用或构建缓存；任何缓存相关处理都须先说明与已证实原因的关系及影响范围。
- 若必要修复涉及后端、接口契约变更或新增依赖，报告证据、所需越界内容与原因，等待用户另行授权；继续完成不依赖越界操作的诊断工作。
- 本机目前无法启动浏览器。不得把源码检查、构建成功或自动化测试通过表述为真实浏览器中已复现问题或修复已验收。

### 诊断顺序

1. 查阅仓库说明与现有配置，定位资料页、保存处理、相关状态管理及数据显示链路，记录实际文件路径和关键符号。文件位置、包管理器和测试命令均未知，须从仓库发现，不得编造。
2. 沿实际代码追踪“提交的值 → 保存请求与响应 → 前端状态更新 → 页面渲染”，核对现有成功判定及数据来源。将缓存、状态同步或请求时序等视为候选假设，仅跟进有证据支持的分支；说明每条证据能支持或排除什么。
3. 查找已有日志、测试、运行记录和可用执行环境。复现步骤尚未稳定时，整理已知触发条件及缺失信息；发现相关脚本后再运行针对性的现有检查，记录实际命令、结果和限制。
4. 汇总已确认事实、根因假设及证据缺口。若现有证据不足以确定修复依据，报告还需哪些最少信息，例如保存请求/响应、出现旧值时的页面状态或可靠复现步骤，并明确哪些修复工作必须等待这些信息；不得因缺少浏览器而虚构复现。

### 条件满足后的最小修复

只有当证据足以支持根因和对应改动、且改动完全属于前端范围时，才进行聚焦修复。修改前说明证据如何指向该改动，复用项目现有实现方式，不做无关重构。即使能据此修复可确认的逻辑问题，仍须保留真实浏览器验收缺口。

### 验证与交付

- 按实际改动选择适度验证；如需回归测试，使用现有工具验证故障机制及修复效果，不新增依赖或要求无关全量检查。测试、静态检查和构建各自只证明其覆盖范围。
- 留下可在可用浏览器环境中执行的验证步骤：按发现的触发条件保存资料，核对成功后的显示值与已确认保存值是否一致、是否仍需刷新；间歇性问题应记录观察条件与结果，不以单次正常操作断言已消除。
- 最终报告诊断结论与证据锚点、修改文件及理由（如有）、实际执行命令和结果、剩余信息需求及阻塞。分别标明“计划验证”“已验证（注明范围）”“未验证/阻塞（注明原因）”。
- 本机浏览器不可用期间，真实浏览器复现与修复验收必须标为未验证/阻塞。若未修复，明确说明证据不足或边界限制；若已做代码修复，明确说明已完成的验证范围和仍待浏览器验收的内容。
```

### C：根因不明（修订后复测）

输出 LF-SHA256：`7c7e4f9791eedb27e8e6fb459982b86cc5f98583ff7a47504985ca36d6221f1d`。

```text
请先诊断“资料页保存后偶尔仍显示旧值、刷新后恢复”的问题，在证据支持根因后实施最小前端修复。

已知与边界：
- 以上是用户报告的症状，尚无日志或稳定复现。“肯定是缓存、想清缓存”是用户的推测，不能直接当作根因或修复指令。
- 相关文件、接口和测试命令均未知；本机目前无法启动浏览器。
- 只允许修改前端，不准修改后端或增加依赖。若解决问题需要越界，报告所需变更、依据及受影响的后续工作，停止越界部分，继续可独立完成的范围内工作。

先做发现和诊断：
1. 阅读仓库约定，确认工作区已有修改、项目结构及可用环境；定位资料页、保存入口、保存后的状态更新与重新读取流程，以及实际使用的接口契约。查明现有脚本和测试工具后再决定运行命令，不编造路径、接口或调用方式。
2. 梳理从编辑、提交、响应到页面展示的数据流，区分已观察事实与假设。缓存、前端状态未同步、请求时序或响应处理均可作为候选解释；按实际证据筛选，说明如何证实或排除。
3. 优先利用现有日志、测试和可用运行环境补充证据。允许在上述范围内做可逆、定向的诊断实验，例如临时观测或控制请求顺序；注明实验条件与结果，交付前清理不必要的实验改动。实验可以检验假设，不能把尚未证实的清缓存方案直接留作修复。
4. 若仍无法确定根因，提交已完成的检查、候选原因及证据缺口，并列出下一步所需的最少信息或复现条件。需要用户提供的关键信息应形成明确问题，并说明哪部分工作必须等待回答；不要用猜测补齐。

有证据后再修复：
- 先给出从触发条件、错误数据流到旧值展示的证据链，附实际文件/符号、日志或实验结果等定位依据，并说明证据强度和限制。仅有代码上的可疑点不足以认定本次故障根因。
- 证据足够时，在授权范围内完成与根因对应的最小修改，遵循已有实现方式，保留无关行为与现有工作，避免顺带重构。

验证与交付：
- 从实际项目发现并运行与变更相关的检查；若已有测试条件允许，针对识别出的故障路径补充或复用有意义的回归验证。报告实际命令、结果与覆盖范围。
- 待浏览器可用后，按诊断得到的触发条件验证：保存成功后，资料页无需刷新即可显示本次保存后的正确值；随后刷新应保持一致。间歇性问题需说明尝试条件、次数和观察结果，不能凭单次未出现就断言彻底解决。
- 本机浏览器不可用期间，浏览器复现与交互验收标为“阻塞/未验证”，记录原因及待执行步骤。源码检查、构建或测试通过只证明各自覆盖的内容，不能算作原问题已复现或修复已通过浏览器验收。
- 最终交付诊断结论与证据、实际修改及其理由（若有）、已执行验证、未验证项与后续步骤。明确区分“根因未确认”“已修复但待浏览器验收”等实际状态；无充分证据时交付诊断结果，不宣称已修复。
```
